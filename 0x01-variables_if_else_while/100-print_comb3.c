#include <stdio.h>
/**
 * main - main mdescription
 *
 * Return: always (0)
 */
int main(void)
{
        char d;
        char n


        for (d = 'a'; d <= 'f'; d++)
        {
                for (n = '0'; n <= '9'; n++)
                {
                        putchar(d + n);
                }
                if (j != 9)
                {
                        putchar(',');
                        putchar(' ');
                }
        }
        }
        putchar('\n');
        return (0);
}
