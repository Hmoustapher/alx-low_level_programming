#include <stdio.h>
/**
 * main - m these is terlifj
 *
 * Return: always (0)
 */
int main(void)
{
	int n = 0;
	char t = 'a';

	while (n <= '0')
	{
		putchar(n);
		n++;
	}
	while (t <= 'f')
	{
		putchar(t);
		t++;
	}
	putchar('\n');
	return (0);
}
